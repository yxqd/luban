#!/usr/bin/env python
#
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#
#                                 Jiao Lin
#                      California Institute of Technology
#                      (C) 2006-2009  All Rights Reserved
#
# {LicenseText}
#
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
#


import luban.content
from luban.content import select, load, alert


from aokuang.components.WidgetDemoActor import Actor as base, panel, example, quickpanel
class Actor(base):


    from luban.content.ReStructuredTextDocument import ReStructuredTextDocument as widget    

    class Inventory(base.Inventory):

        import pyre.inventory

    
    introduction = '''
This is a demo of widget "restructuredtextdocument".

Widget "restructuredtextdocument" displays
restructuredtext document.
'''

    @quickpanel(
        title = 'Basic usages',
        description =  [
          ],
        examples = ['basic_usage%s' % (i+1) for i in range(1)],
        )
    def basic_usages(self): pass


    @example(
        title='A ReStructuredText document',
        description = [
          ]
        )
    def basic_usage1(self, director):
        text = '''
Title here
==========

Some more items

* a
* b
        '''
        return luban.content.restructuredtextdocument(text=text)


    # obsolete
    def test_obsoleteinterface(self, director):
        lc = luban.content
        
        frame = lc.frame(title='Testing ReStructuredTextDocument')
        document = frame.document(title='Testing ReStructuredTextDocument')

        from luban.content.ReSTDocument import ReSTDocument
        text = '''
Title here
==========

Some more items

* a
* b
        '''
        reST = ReSTDocument(text=text)
        document.add(reST)
        
        return frame


def actor():
    return Actor('restructuredtext')


# version
__id__ = "$Id$"

# End of file 
