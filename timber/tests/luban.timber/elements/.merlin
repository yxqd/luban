# -*- python -*-

import merlin.rules, os
from merlin.Context import create
context = create()
context.code.project = 'luban.timber'


tests = []
import glob
seq = open('.sequence').readlines()
for f in seq:
    f = f.strip()
    tests += glob.glob(f)
    continue


rules = []
rules.append(
    merlin.rules.testSuite(name="luban.timber.elements", tests = tests)
    )


recurse_entries = [
    ]
